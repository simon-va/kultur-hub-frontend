import { Route } from '@angular/router';
import { LoginPage } from './auth-feature-login/auth-feature-login';

export const authFeatureLoginRoutes: Route[] = [
  { path: '', component: LoginPage }
];
