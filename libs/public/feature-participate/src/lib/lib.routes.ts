import { Route } from '@angular/router';
import { PublicFeatureParticipate } from './public-feature-participate/public-feature-participate';

export const publicFeatureParticipateRoutes: Route[] = [
  { path: '', component: PublicFeatureParticipate },
];
