import { Route } from '@angular/router';
import { PublicFeatureReports } from './public-feature-reports/public-feature-reports';

export const publicFeatureReportsRoutes: Route[] = [
  { path: '', component: PublicFeatureReports }
];
