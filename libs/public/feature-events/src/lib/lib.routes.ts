import { Route } from '@angular/router';
import { PublicFeatureEvents } from './public-feature-events/public-feature-events';

export const publicFeatureEventsRoutes: Route[] = [
  { path: '', component: PublicFeatureEvents }
];
