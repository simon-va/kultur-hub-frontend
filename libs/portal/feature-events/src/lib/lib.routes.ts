import { Route } from '@angular/router';
import { PortalFeatureEvents } from './portal-feature-events/portal-feature-events';

export const portalFeatureEventsRoutes: Route[] = [
  { path: '', component: PortalFeatureEvents }
];
