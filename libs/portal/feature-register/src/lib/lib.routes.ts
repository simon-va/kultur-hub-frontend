import { Route } from '@angular/router';
import { RegisterPage } from './portal-feature-register/portal-feature-register';

export const portalFeatureRegisterRoutes: Route[] = [
  { path: '', component: RegisterPage }
];
